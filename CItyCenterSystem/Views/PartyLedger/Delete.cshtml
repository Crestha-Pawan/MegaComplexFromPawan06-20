@model FiboInfraStructure.Entity.Ledger.Ledger

@{ Layout = null; }

<form asp-action="DeleteConfirmed" asp-controller="Ledger">
    @Html.AntiForgeryToken()
    @Html.HiddenFor(x => x.Id)
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">@Model.PartyId</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        </div>
        <div class="modal-body">
            <p>Are You Sure to Delete @Model.PartyId?</p>
        </div>
        <div class="modal-footer">
            <button type="submit" class="btn  btn-danger">Delete Confirm</button>
            <button type="button" class="btn  btn-secondary" data-dismiss="modal">Cancel</button>
        </div>
    </div>
</form>